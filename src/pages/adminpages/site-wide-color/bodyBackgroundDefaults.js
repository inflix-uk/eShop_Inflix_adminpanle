/** Default body background when no CMS value is set. */
export const DEFAULT_BODY_BG = "#ffffff";

/** Native `<input type="color">` only accepts #rrggbb; use neutral when value is invalid. */
export const COLOR_PICKER_FALLBACK = "#9ca3af";
